################################################################################
# Mystic Home Server - Environment Configuration
#
# Copy this file to .env and customize:
#   cp .env.example .env
#
# Docker Compose reads .env automatically from the compose file directory.
################################################################################

# Volume root directory
# Default: . (current directory — wherever docker-compose.yml lives)
# Set absolute path if volumes live elsewhere (e.g., /mnt/storage/docker)
MYSTIC_ROOT=.
